# Handoff — 2026-07-21 — depup.io AWS migration: task premise was stale (already done)

_Status: COMPLETED (no-op — nothing to build; verified true state + surfaced 1 open decision)_

## Goal (as tasked)
Execute the depup.io portion of the AWS domain-consolidation plan: rebuild/deploy the site
to S3+CloudFront, create a Route53 zone + stage SES MX/SPF/DKIM, set up Cloudflare Email
Routing forwarding as interim path — then produce a ready/not-ready summary for Mikl's
one-tap approval on the NS flip (explicitly do NOT flip NS).

## Outcome: the migration was already complete — no orchestration run
Every step the task asked for was already live (done ~2026-06-08, a month+ prior). I did
NOT spin up the planned 3 Sonnet + 1 Haiku agents — doing so would have created a duplicate
hosted zone (different NS set) and risked taking live email down. Verified the real state
with read-only DNS + AWS calls instead. This independently re-confirms the 2026-07-18
finding (`project_depup_domain_migration_state.md`) — two sessions now agree.

## Verified live state (read-only, 2026-07-21)
- **NS already on AWS**, not Cloudflare — four `awsdns-*` nameservers resolve for depup.io.
  The "flip from Cloudflare" the task guards against already happened. Nothing to flip.
- **Route53 hosted zone exists** (9 records, created 2026-06-08 with a `...stage...` reference).
- **SES domain identity = `Success`** (us-east-1). MX → SES inbound, SPF TXT, `_amazonses`
  verification TXT, 3 DKIM CNAMEs — all live. Inbound mail forwards to devdepup@gmail.com
  via a Lambda forwarder (per prior note).
- **Cloudflare Email Routing forwarding** was also already set up (`cf2024-1._domainkey` TXT
  present) — the plan's "set up forwarding FIRST" interim step is done.

## The one genuine gap (open decision — deferred to Mikl)
- **No web record for apex/www** — depup.io serves email only, no browser page.
- Repo site is a single static `docs/index.html` built for **GitHub Pages**; there is **no
  S3/CloudFront** for it.
- Decision surfaced in-thread; Mikl `/close`d without answering. Options: point the domain at
  the existing GitHub Pages site (fast, free, matches the build — recommended) vs. stand up
  new S3+CloudFront (what the stale task assumed). Both non-destructive; either can be driven
  fully once picked.

## Why the task premise was stale
- The cited plan `docs/plans/2026-06-07-aws-domain-consolidation.md` does **not exist in the
  depup repo** — it lives in the **life repo**
  (`~/Nextcloud/mikmac/life/docs/plans/2026-06-07-aws-domain-consolidation.md`). The task
  assumed it was here. Detail on the migrated state is in
  `docs/context/2026-07-18-depup-io-aws-migration-state.md` (depup repo).

## Next session
1. **(Mikl)** Pick website hosting (GitHub Pages vs S3+CloudFront). Non-blocking; email is fine.
2. Optional housekeeping (non-blocking): drop the stale `cf2024-1` DKIM TXT + the inactive
   `depup-inbound` SES ruleset.
3. **Unrelated but higher priority:** the factory is still offline on the expired `NPM_TOKEN`
   (see 2026-07-19 handoff) — Mikl-only rotation. Nothing publishes until then.

## Context that would be lost
- Memory updated (configs repo, `project: depup`): `project_depup_domain_migration_state.md`
  re-verified/date-bumped to 2026-07-21 + added the "plan is in life repo, don't create a 2nd
  zone" gotcha and the deferred website decision. Index line in `MEMORY.md` updated to match.
  No new file created — folded into the existing single source of truth to avoid index bloat.
- No code changed this session (pure read-only investigation). No resources spun up. 5 pre-
  existing factory-churn stashes left untouched (not this session's, already flagged disposable
  in the 2026-07-19 handoff).
- Gotcha reconfirmed: `git status` in this working tree times out — use targeted git commands.
